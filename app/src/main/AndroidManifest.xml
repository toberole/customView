<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhouwei.customview">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ParallaxListviewActivity" />
        <activity android:name=".activity.SpannableStringActivity" />
        <activity android:name=".activity.SpannableStrActivity" />
        <activity android:name=".activity.BesselLoadingViewActivity" />
        <activity android:name=".activity.SwipeLayoutActivity"></activity>
    </application>

</manifest>